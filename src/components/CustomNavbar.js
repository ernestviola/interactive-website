import React, { Component } from 'react';

class CustomNavbar extends Component {
    render() {
        return (
            <nav>
                
            </nav>
        );
    }
}

export default CustomNavbar;